# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true

scenarios:
  - name: "Robustness tests for GetAllProducts API"
    flow:
      # Invalid Authorization Header
      - post:
          url: "/products"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing with invalid authorization header expecting 401"
          expect:
            - statusCode: 401

      - think: 1

      # Missing Required Field (e.g. 'name' in response)
      - get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing without required response fields expecting 400"
          expect:
            - statusCode: 400

      - think: 1

      # Invalidation of the URL
      - get:
          url: "/invalid-url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing with invalid URL expecting 404"
          expect:
            - statusCode: 404

      - think: 1

      # Testing with Invalid Method
      - put:
          url: "/products"
          log: "Testing invalid HTTP method PUT expecting 405"
          expect:
            - statusCode: 405

      - think: 1

      # Invalid Accept Header
      - get:
          url: "/products"
          headers:
            Accept: "application/xml"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing with invalid 'Accept' header expecting 406"
          expect:
            - statusCode: 406
            - hasHeader: "Content-Type"

      - think: 1

      # Testing with Unsupported Content-Type on POST
      - post:
          url: "/products"
          headers:
            Content-Type: "application/xml"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing with unsupported Content-Type header on a POST request expecting 415"
          expect:
            - statusCode: 415

      - think: 1

      # Invalid Payload: Missing Required Fields
      - post:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            # Intentionally missing required fields like "id" and "name"
            description: "Invalid payload without id and name"
            price: 20
          log: "Testing with incomplete JSON payload missing required fields expecting 422"
          expect:
            - statusCode: 422

