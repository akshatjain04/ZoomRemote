# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_get_others.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test various failure scenarios for GET /products"
    flow:
      - log: "Testing with invalid URL"
      - get:
          url: "/invalidProductsEndpoint"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Testing with missing required fields in response"
      - get:
          url: "/products"
          expect:
            - statusCode: 500
            - hasProperty: "name"
      - think: 1

      - log: "Testing with invalid Authorization header"
      - get:
          url: "/products"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Testing with malformed request payload"
      - get:
          url: "/products"
          json:
            id: "NaN"
            name: 123
            description: true
            price: "free"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Testing with unexpected content type in response"
      - get:
          url: "/products"
          headers:
            Accept: "text/plain"
          expect:
            - statusCode: 406
      - think: 1

      - log: "Testing with server error"
      - get:
          url: "/products"
          expect:
            - statusCode: 500
      - think: 1

      - log: "Testing with missing response header"
      - get:
          url: "/products"
          expect:
            - statusCode: 200
            - "!hasHeader": "X-Custom-Header"
