# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Delete Product Error Handling Scenario"
    flow:
      # Invalid Product ID
      - log: "Testing with invalid product ID"
      - think: 1
      - delete:
          url: "/products/invalidId"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: 'description'
      # Missing Product ID
      - log: "Testing with missing product ID"
      - think: 1
      - delete:
          url: "/products/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: 'content-type' 
      # Valid Product ID but with no authorization
      - log: "Testing valid product ID with no authorization header"
      - think: 1
      - delete:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 404
            - hasHeader: 'content-length'
      # Valid Product ID but with invalid authorization token
      - log: "Testing valid product ID with invalid authorization token"
      - think: 1
      - delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "InvalidAuthToken"
          expect:
            - statusCode: 404
            - hasProperty: 'description'
      # Validation failure with invalid URL
      - log: "Testing invalid URL"
      - think: 1
      - delete:
          url: "/invalidUrl"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: 'content-type'

