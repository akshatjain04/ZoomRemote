# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=dcd186097b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payloads:
    - path: "products_post_others.csv"
      fields:
        - "id"
        - "name"
        - "description"
        - "price"
      skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Invalid inputs and failure scenarios for POST /products"
    flow:
      # Invalid payload missing mandatory 'name' field
      - post:
          url: "/products"
          json:
            id: "{{ id }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing without mandatory 'name' field, expecting 405 error"
          expect:
            - statusCode: 405
        
      - think: 1

      # Submit invalid price type (string instead of integer)
      - post:
          url: "/products"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "InvalidPriceType"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing with invalid price type, expecting 405 error"
          expect:
            - statusCode: 405
        
      - think: 1

      # Submit valid data with an invalid authentication token
      - post:
          url: "/products"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Content-Type: "application/json"
            Authorization: "InvalidToken"
          log: "Testing with invalid authentication token, expecting authorization failure response"
          expect:
            - statusCode: 405
            - hasHeader: "www-authenticate" # This header is typically used to indicate authentication failure
        
      - think: 1

      # Validation failure by using an invalid URL
      - post:
          url: "/invalidURL"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing with an invalid URL, expecting a 405 error"
          expect:
            - statusCode: 405

