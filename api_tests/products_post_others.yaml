# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=dcd186097b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_post_others.csv"
    fields:
      - "invalid_name"
      - "invalid_price"
    skipHeader: true
  environments:
    production:
      target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}

scenarios:
  - name: "Create new product error scenarios"
    flow:
      # Send a request with an invalid 'price' field (negative number)
      - log: "Sending a request with an invalid price"
      - post:
          url: "/products"
          headers:
            content-type: "application/json"
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          payload: 
            name: "apple"
            description: "apple"
            price: "{{ invalid_price }}"
          # Expect a 405 status code for invalid input
          expect:
            - statusCode: 405

      - think: 1
      
      # Send a request with missing 'name' field (required)
      - log: "Sending a request without the required name field"
      - post:
          url: "/products"
          headers:
            content-type: "application/json"
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            description: "apple"
            price: 50
          # Expect a 405 status code for invalid input
          expect:
            - statusCode: 405

      - think: 1

      # Send a request with an invalid endpoint URL
      - log: "Sending a request to an invalid URL"
      - post:
          url: "/invalid_products"
          headers:
            content-type: "application/json"
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            name: "apple"
            description: "apple"
            price: 50
          # Expect a status code != 200-299 for the invalid URL
          expect:
            - statusCode: 405

      - think: 1

      # Send a request with an invalid authorization header
      - log: "Sending a request with an invalid auth token"
      - post:
          url: "/products"
          headers:
            content-type: "application/json"
            authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            name: "apple"
            description: "apple"
            price: 50
          # Expect a status code that indicates authorization failure
          expect:
            - statusCode: 405

      - think: 1

