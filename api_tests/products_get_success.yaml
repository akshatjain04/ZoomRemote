# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  plugins:
    expect: {}

scenarios:
  - name: "Ensure successful retrieval of all products"
    flow:
      - get:
          url: "/products"
          # Comment: Perform a GET request to retrieve all products, expecting a 200 successful status.
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: 'id'
            - hasProperty: 'name'
            - hasProperty: 'description'
            - hasProperty: 'price'
          log: "Retrieve all products to ensure proper functioning of GET /products"
      - think: 1

