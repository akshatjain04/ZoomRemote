# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /products_get for http method type GET in artillery framework
# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "products_get_success.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true

scenarios:
  - name: "Get All Products Scenario"
    flow:
      - log: "Retrieving all products"
      - get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasHeader: "Content-Type"
          capture:
            json: "$.id"
            as: "product_id"
      - think: 1

      - log: "Retrieved 2 products"
      - get:
          url: "/products/{{ product_id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - hasHeader: "Content-Type"
      - think: 1

      - log: "Retrieving product by ID: {{ product_id }}"
      - get:
          url: "/products/{{ product_id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - hasHeader: "Content-Type"
      - think: 1

      - log: "Product not found"
      - get:
          url: "/products/99999" # Assuming 99999 is an ID that does not exist
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - hasHeader: "Content-Type"
      - think: 1

      - log: "Retrieved 1 products"
      - get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasHeader: "Content-Type"
      - think: 1
