# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  plugins:
    expect: {}

scenarios:
  - scenario: "Get All Products"
    flow:
      - get:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          # Get all products - Expect a 200 status code and valid product data structure
          log: "Requesting all products."
          think: 1
          capture:
            json: "$"
            as: "responseBody"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasHeader: "Content-Type"

      - log: "Retrieved list of all products successfully."
        think: 1

