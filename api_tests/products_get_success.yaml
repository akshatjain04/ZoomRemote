# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_get_success.csv"
    fields:
      - "name"
      - "price"
      - "description"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Get All Products scenario"
    flow:
      - log: "Sending a request to retrieve all products."
      - get:
          url: "/products"
          # Authorization is not specified in the spec, so we do not include it.
          # Expected behavior: The service should return a list of all products.
          headers:
            Content-Type: "application/json"
          capture:
            json: "$.body"
            as: "responseBody"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasHeader: "Content-Type"
          afterResponse: "logResponse"
      - think: 1

functions:
  logResponse:
    - log: "Received a response with products."

