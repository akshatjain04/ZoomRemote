# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_productId_delete_success.csv"
    fields:
      - "productId"
    skipHeader: true
  plugins:
    expect: {}
  environments:
    staging:
      target: "https://staging.example.com"
      phases:
        - duration: 300
          arrivalCount: 10

scenarios:
  - name: "Delete product scenario"
    flow:
      - comment: "Delete a product with a given product ID"
        post:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "Product deleted"
          log: "Sending request to delete product with ID {{ productId }}"
        think: 1
      - comment: "Attempt to delete the same product again, should receive 404 not found"
        post:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "Not Found"
          log: "Sending request to delete product with ID {{ productId }} which should already be deleted"
        think: 1

