# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_productId_get_others.csv"
    fields:
      - "productId"
      - "invalidProductId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Negative testing for getProductById endpoint"
    flow:
      - log: "Sending request with non-existing productId to expect 404 response"
      - think: 1
      - get:
          url: "/products/{{ invalidProductId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      # Product with the ID does not exist, expect a 404 Product not found response

      - log: "Sending request with malformed productId to expect 404 response"
      - think: 1
      - get:
          url: "/products/malformedid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      # ProductId is malformed (not an integer), expecting a 404 Product not found response due to invalid route

      - log: "Sending request with empty productId to expect 404 response"
      - think: 1
      - get:
          url: "/products/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      # Empty productId in the path, expecting a 404 Product not found response due to invalid route

      - log: "Sending request with invalid Authorization token to expect authentication failure"
      - think: 1
      - get:
          url: "/products/{{ invalidProductId }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
      # Invalid Authorization token provided, expecting a 401 Unauthorized response

