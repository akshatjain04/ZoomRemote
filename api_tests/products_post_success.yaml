# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=dcd186097b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_post_success.csv"
    fields:
      - "name"
      - "description"
      - "price"
    skipHeader: true
  environments:
    local:
      API_HOST: "http://localhost:3000"
  plugins:
    expect: {}
  
scenarios:
  - name: "Create New Product"
    flow:
      # Create new product with valid data from CSV
      - post:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          log: "Creating a new product with valid inputs from CSV"
          think: 1
          # Expecting a successful creation of the product
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasHeader: "content-type"
          # Comment on expected behavior
          comment: "The request should create a new product with provided name, description, and price and return a 200 response."

      # Create a new product with fixed data as per examples in the spec
      - post:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "apple"
            description: "apple"
            price: 50
          log: "Creating a new product with fixed example inputs"
          think: 1
          # Expecting a successful creation of the product
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            - hasHeader: "content-type"
          # Comment on expected behavior
          comment: "The request should create a new product with example name, description, and price and return a 200 response."

