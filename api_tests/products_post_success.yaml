# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=dcd186097b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "products_post_success.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  env:
    AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Create new product"
    flow:
      - post:
          url: "/products"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          beforeRequest: "log"
          log: "Sending request to create a product with the name: {{ name }} and price: {{ price }}"
          think: 2
          afterResponse: "log"
          expect:
            - statusCode: 200
            - hasProperty: 'id'
            - hasProperty: 'name'
            - hasProperty: 'description'
            - hasProperty: 'price'
          logResponse: "Response received. Checking the response for product creation with name: {{ name }}"
      - think: 2

