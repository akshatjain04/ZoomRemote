# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "products_productId_get_success.csv"
    skipHeader: true
    fields:
      - "someProductId"
  plugins:
    expect: {}

scenarios:
  - name: "Verify successful retrieval of product details by product ID"
    flow:
      - log: "Testing retrieval of product with ID {someProductId}"
      - get:
          url: "/products/{{ someProductId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: 'name'
            - hasProperty: 'id'
            - hasProperty: 'description'
            - hasProperty: 'price'
            - hasHeader: 'content-type'
          capture:
            json: "$.price"
            as: "valid_price"
      - think: 1

      # In the following request, we will use the previous response's price to ensure that price is correctly captured
      - log: "Asserting that retrieved price is identical to expected value for product ID {someProductId}"
      - get:
          url: "/products/{{ someProductId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - equals: { body: { price: "{{ valid_price }}" }}
      - think: 1

